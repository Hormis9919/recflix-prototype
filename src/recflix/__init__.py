# Module: recflix_alpha
# Purpose: Package initializer for the `recflix_alpha` experimental model.
#
# This file marks the `recflix_alpha` folder as a Python package. It can
# also be used to expose convenient top-level imports for the package's
# public API (e.g., `from src.recflix_alpha import train, evaluate`).
#
# Currently this package relies on explicit module imports elsewhere in
# the project rather than defining a public import surface here.
